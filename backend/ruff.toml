line-length = 150
target-version = "py311"

[lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["B008", "UP042"]  # FastAPI Depends()/File() defaults; keep (str, Enum) for SQLAlchemy

[lint.per-file-ignores]
"app/migrations/versions/*" = ["E501"]
