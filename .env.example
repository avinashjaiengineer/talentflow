# --- Sign-in ---------------------------------------------------------------
# The first admin account, created on startup when there are no users yet.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-please
# Sessions are signed with SECRET_KEY. In development a random key is generated on
# each start (you'll be signed out on restart); set one to stay signed in.
# SECRET_KEY=

# --- LLM -------------------------------------------------------------------
# Without a key, TalentFlow runs in offline mode with heuristic agents.
ANTHROPIC_API_KEY=
# auto | anthropic | mock
LLM_PROVIDER=auto
DEFAULT_MODEL=claude-opus-5
# Optional per-agent overrides, e.g. a cheaper model for high-volume steps:
# MODEL_SOURCING=claude-haiku-4-5
# MODEL_SCREENING=claude-sonnet-5
# MODEL_OUTREACH=claude-sonnet-5
# MODEL_SCHEDULING=claude-haiku-4-5
# MODEL_EVALUATION=claude-opus-5
# MODEL_INTAKE=claude-haiku-4-5
# MODEL_JOB_WRITER=claude-sonnet-5
# low | medium | high | xhigh | max
LLM_EFFORT=high

# --- Database --------------------------------------------------------------
# Docker Compose sets this to its bundled Postgres + pgvector automatically.
# For local dev without Postgres, the SQLite default just works.
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/talentflow
# DATABASE_URL=sqlite:///./talentflow.db

# --- Embeddings ------------------------------------------------------------
# fastembed (local, no key) | voyage (hosted) | hash (tests only)
EMBEDDING_PROVIDER=fastembed
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIM=384
# Resumes are embedded in pieces of about this many characters (fits every model's input limit).
# EMBEDDING_CHUNK_CHARS=1500
# Better local quality: EMBEDDING_MODEL=BAAI/bge-base-en-v1.5 with EMBEDDING_DIM=768.
# After changing the provider, model, or dimension, run: python -m app.reembed
# For Voyage AI (hosted; Anthropic's recommended embeddings):
# EMBEDDING_PROVIDER=voyage
# EMBEDDING_MODEL=voyage-3.5
# EMBEDDING_DIM=1024
# VOYAGE_API_KEY=

# --- Company ---------------------------------------------------------------
COMPANY_NAME=Acme Corp
# IANA timezone for interview slots, e.g. America/New_York, Europe/London, Asia/Kolkata
TIMEZONE=UTC
INTERVIEW_DURATION_MINUTES=45

# --- Email, calendar, Teams, AI calls (see docs/INTEGRATIONS.md) -----------
# All off by default: nothing reaches a candidate until a provider is connected.
EMAIL_PROVIDER=outbox        # outbox | graph (Outlook)
CALENDAR_PROVIDER=local      # local | graph (Outlook free/busy + Teams meetings)
# MS_TENANT_ID=
# MS_CLIENT_ID=
# MS_CLIENT_SECRET=
# MS_SENDER=recruiting@yourcompany.com
VOICE_PROVIDER=simulated     # simulated | twilio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=+14155550100
# PUBLIC_BASE_URL=https://talent.yourcompany.com   # required for Twilio (https)
# MODEL_CALLER=claude-haiku-4-5                    # fastest replies on live calls
# REMINDER_HOURS_BEFORE=24

# --- Job portal intake (see docs/INTEGRATIONS.md) ---------------------------
# Read applications that Naukri, LinkedIn, Indeed, etc. email to an Outlook mailbox.
INTAKE_PROVIDER=off          # off | graph (uses the MS_* settings above; needs Mail.Read)
# INTAKE_MAILBOX=jobs@yourcompany.com   # defaults to MS_SENDER
# INTAKE_FOLDER=inbox
# INTAKE_POLL_MINUTES=15
# INTAKE_LOOKBACK_DAYS=7
# INTAKE_AUTO_SCREEN=true
# Enables POST /api/intake/webhook for careers pages and tools like Zapier.
# INTAKE_WEBHOOK_TOKEN=

# --- Original resume files ----------------------------------------------------
STORAGE_PROVIDER=local       # local | s3 (AWS S3, Cloudflare R2, MinIO) | none
# STORAGE_DIR=./data/files
# S3_BUCKET=talentflow-resumes
# S3_ENDPOINT_URL=https://<account-id>.r2.cloudflarestorage.com   # R2 or MinIO only
# S3_REGION=auto                                                  # "auto" for R2
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
